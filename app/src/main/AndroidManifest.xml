<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="jgonzalezt.motorina">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_app"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Motorina">
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PromedioActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name=".activity.RegistroLecturaActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".activity.RegistroActivity" />
        <activity
            android:name=".activity.RegistroEquipoActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>